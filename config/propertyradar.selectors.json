{
  "_README": [
    "Selectors for the optional PropertyRadar Sold/Listed verification step.",
    "Only used when CHECK_PROPERTYRADAR=true in .env.",
    "",
    "Purpose: before texting, look the property up in PropertyRadar and confirm",
    "it is not Sold or Listed. If PropertyRadar says sold/listed, the lead is",
    "dispositioned accordingly and NOT texted. If PropertyRadar can't be read,",
    "the REI BlackBook status is used instead (this step never forces a text).",
    "",
    "STATUS: NEEDS-DOM everywhere below - these are best-guess placeholders from",
    "the Discover screenshot. Verify each against the live PropertyRadar account",
    "before enabling. Playwright selector engines allowed: CSS, 'text=...', ':has-text()'."
  ],

  "login": {
    "_status": "PARTIAL - logged-in marker confirmed ('Discover'). Login page fields still a best guess.",
    "loginUrl": "https://www.propertyradar.com/login",
    "emailInput": "input[type='email'], input[name='email']",
    "passwordInput": "input[type='password'], input[name='password']",
    "submitButton": "button[type='submit'], button:has-text('Sign In'), button:has-text('Log In')",
    "loggedInMarker": "text=Discover"
  },

  "search": {
    "_status": "PARTIAL - CONFIRMED: 'Full Address' quick-search tab and the search box on Discover. The property-detail page (after opening a result) still needs a capture to confirm where Sold/Listed shows.",
    "fullAddressTab": "text=Full Address",
    "addressInput": "input[placeholder*='Address' i], input[placeholder*='City' i], input[placeholder*='ZIP' i]",
    "submit": "button:has-text('Search'), .search-btn",
    "resultRow": ".result-row, table tbody tr, .property-card",
    "resultLink": ".result-row a, table tbody tr a, .property-card a",
    "noResultsMarker": "text=No results, text=No properties found"
  },

  "status": {
    "_status": "NEEDS-DOM - property detail status. Capture a sold and a listed property to confirm.",
    "container": ".property-status, .status-section",
    "soldMarker": "text=Sold, text=Last Sale",
    "soldDateField": "[data-field='sale-date'], .sale-date",
    "listedMarker": "text=Listed, text=For Sale, text=Active Listing, text=On Market",
    "listingField": "[data-field='listing-status'], .listing-status"
  }
}
